<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>My Gallery</title>
    <link href="/css/style.css" rel="stylesheet" type="text/css" />
  </head>
  <body>
    <div id="logo">
      <a href="home">
        <img src="/logo5.png" alt="Logo" class="logoImg" />
        <img src="/logo4.png" alt="Logo" class="logoImg" />
      </a>
    </div>
    <div class="parent">
      <div class="box2">
        <div class="grid-container">
          <% if (images.length > 0) { %> <% images.reverse().forEach((image) =>
          { %>
          <div class="grid-item">
            <a href="/photos/<%= image.id %>">
              <img
                src="<%= image.url %>"
                alt="<%= image.filename %>"
                id="gridImg"
                style="max-width: 200px"
              />
            </a>
            <p class="uploader-info"><%= image.uploader %></p>
            <p class="uploader-info"><%= image.upload_time %></p>
          </div>
          <% }); %> <% } else { %>
          <p>It looks too empty here, why don't you upload something?</p>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
